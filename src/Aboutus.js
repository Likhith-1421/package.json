
import Likhith from "./Components/Likhith";
import useOffline from "./useOnline";
const Aboutus = () => {
  // console.log("parent");
  // const offline = useOffline();
  // if (offline === false) return <h3>u are offline</h3>;
  return <Likhith />;
};

export default Aboutus;
